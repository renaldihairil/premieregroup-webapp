# OS / editor
Thumbs.db
.DS_Store
*.log
.vscode/
.idea/

# nothing is built in Phase 1 — no node_modules / dist yet
node_modules/
dist/

# secrets & environment — NEVER commit
.env
.env.local
.env.production

# backend deps & runtime (added in the stabilization phase)
vendor/
*.sqlite

# storage: keep the folder skeleton, ignore everything written at runtime
/storage/uploads/**
/storage/cache/**
/storage/sessions/**
/storage/keys/**
!/storage/.htaccess
!/storage/**/.gitkeep
/storage/.setup-lock
